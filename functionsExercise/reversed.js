/* function reversed(arr) {

    for (let i = 0; i < arr.length; i ++) {
        let element = arr[i];
        
        let reverse = element.toString().split('').reverse().join('');
        
        element == Number(reverse) ? console.log(true) : console.log(false);
    }
}
reversed([32,2,232,1010]); */