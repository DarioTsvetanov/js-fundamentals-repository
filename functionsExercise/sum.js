/* function sum(num) {

    let convert = () => {
        return num.toString();
    };

    let str = convert();

    let evenSum = 0;
    let oddSum = 0;

    for (let i = 0; i < str.length; i ++) {

        if (str[i] % 2 == 0) {
            evenSum += Number(str[i]);
        }
        else {
            oddSum += Number(str[i]);
        }
    }

    console.log(`Odd sum = ${oddSum}, Even sum = ${evenSum}`);
}
sum(1000435); */