/* function negativePositiveNumbers(arr) {
    let orderedArray = [];

    for (let i = 0; i < arr.length; i ++) {

        if (arr[i] < 0) orderedArray.unshift(arr[i]);
        else orderedArray.push(arr[i]);
    }

    for (const number of orderedArray) {
        console.log(number);
        
    }
}
negativePositiveNumbers([3, -2, 0, -1]); */w