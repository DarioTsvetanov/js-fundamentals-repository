/* function slicedArray(arr) {

    let separator = arr.shift();

    let firstElements = arr.slice(0, separator);
    
    let lastElements = arr.slice(arr.length - separator);

    console.log(firstElements.join(' '));
    console.log(lastElements.join(' '));
}
slicedArray([3, 6, 7, 8, 9]) */