/* function processOddNumber(arr) {

    let processedArray = [];

    for (let i = 0; i < arr.length; i ++) {

        if (i % 2 !== 0) processedArray.unshift(arr[i] * 2);
    }

    console.log(processedArray.join(' '));
}
processOddNumber([3, 0, 10, 4, 7, 3]) */