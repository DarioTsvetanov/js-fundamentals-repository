/* function reverseArray(arr) {

    let result = '';

    for (let i = arr.length - 1; i >= 0; i --) {
        result += `${arr[i]} `;
    }
    console.log(result);
    
}
reverseArray(['a', 'b', 'c', 'd', 'e']); */