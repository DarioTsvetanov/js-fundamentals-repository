/* function reverse(num, arr) {

    let result = '';

    for (let i = num; i > 0; i --) {
        result += `${arr[i - 1]} `;
        
    }


    console.log(result);
    
}
reverse(4, [-1, 20, 99, 5]); */