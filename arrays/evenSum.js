/* function evenSum(arr) {

    let numberArray = [];

    for (let i = 0; i < arr.length; i ++) {
        numberArray[i] = Number(arr[i])
    }

    let sum = 0;

    for (let j = 0; j < numberArray.length; j ++) {
        
        if (numberArray[j] % 2 == 0) {
            sum += numberArray[j];
            
        }
    }
    console.log(sum);
    
}
evenSum(['2','4','6','8','10']); */