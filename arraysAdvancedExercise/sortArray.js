/* function sortArray(arr) {

    let sortedArr = arr.sort((a, b) => a.localeCompare(b))

    let doubleSorted = sortedArr.sort((a, b) => a.length - b.length)

    for (const element of doubleSorted) {

        console.log(element);
    }
}
sortArray(["Isacc", "Theodor", "Jack", "Harrison", "George"]); */