/* function fuel(distance, passengersNumber, price) {
    let neededFuel = (distance / 100) * 7;
    neededFuel += passengersNumber * 0.100;
    let money = neededFuel * price;
    console.log(`Needed money for that trip is ${money}lv.`);
    
}
fuel(260, 9, 2.49); */